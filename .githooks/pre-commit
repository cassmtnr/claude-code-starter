#!/bin/sh

echo "Running pre-commit checks..."

# Lint & format check
echo "→ biome check"
bun run check || exit 1

# Unit tests
echo "→ tests"
bun test src/cli.test.ts || exit 1

echo "All pre-commit checks passed."
